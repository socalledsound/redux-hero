import { initialState } from './initalState'
// import { Actions } from './actions.types'

export const monsterReducer = (state = initialState.monster, action) => {
    // Coming soon...
    return state;
  };